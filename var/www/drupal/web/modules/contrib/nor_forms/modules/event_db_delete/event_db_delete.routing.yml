event_db_delete.db_delete_form:
  path: '/event-db-delete-form'
  defaults:
    _title: 'Event Db Delete Form'  # Update title case for consistency
    _form: '\Drupal\event_db_delete\Form\EventDbDeleteForm'
  requirements:
    _permission: 'access content'  # Use _permission instead of _access
    _role: "administrator+content+editor+scientist+manager"