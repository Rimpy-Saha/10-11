service_consultation.consultation_form:
  path: '/service-consultation-form'
  defaults:
    _title: 'Service Consultation Request'  # Update title case for consistency
    _form: '\Drupal\service_consultation\Form\ServiceConsultationForm'
  requirements:
    _permission: 'access content'  # Use _permission instead of _access